package org.example;

import org.example.library.ValidationException;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;

import static org.junit.jupiter.api.Assertions.*;

class ExampleFromSlideTest {

    @Test
    void testThrows(){

//        ValidationException exception = assertThrows(ValidationException.class, () -> athlete1.validate());
//        assertEquals("Name null", exception.getMessage());
    }


}